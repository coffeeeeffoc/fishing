# 验收与试玩记录

验证日期：2026-09-11（Asia/Shanghai）。环境：Windows、Node 24.12、React 19、Vite 7、TypeScript 5.9、独立 Chromium / WebKit 测试上下文及 Codex 实际浏览器。

## 可重复命令

- `npm install`：已实际安装；锁文件已提交。
- `npm run typecheck`：TypeScript 检查。
- `npm run lint`：ESLint。
- `npm test`：14 项逻辑回归，包括 3 个完整 180 秒战术模拟。
- `npm run test:browser`：Chromium 10 场景 + WebKit 9 场景。
- `npm run test:static`：自动生产构建、临时静态托管、子目录及断网检查。
- `npm audit --omit=dev --registry=https://registry.npmjs.org`：运行时依赖 0 漏洞。用户配置的镜像不支持 audit，故查询官方 registry。

运行浏览器测试前执行 `npx playwright install chromium webkit`，保持开发服务器运行。测试脚本源码、夹具与断言均随仓库提交，截图和 JSON 报告生成于 `artifacts/`。

## 实际计时试玩

以下通过实际浏览器点击、按住、移动、切换武器和界面操作完成；没有修改计时或直接增加伤害/得分。

| 轮次 | 视口 | 结果 | 观察与调整 |
|---|---|---|---|
| 第一次互动航行 | 844×390 | 180 秒；14,766 分；310 捕获；1 Boss；最高 Combo 75 | 验证长按、拖动、冰冻/激光、散射/双倍/急速、暂停与恢复；末段过密，随后加入软上限 |
| 第二次互动航行 | 667×375 | 180 秒；14,945 分；304 捕获；0 Boss；最高 Combo 176 | 最小手机横屏完成；使用冰霜和激光；末段仍需追踪 Boss，固定射线无法保证击杀 Boss |
| 生产流程 | 852×393 | 180 秒；5,023 分；158 捕获；最高 Combo 63 | 前半局射击，后半局停止射击，计时和结算仍正常；无开发面板或调试对象；控制台无错误 |

第一、二局结算后金库合计 29,711，游戏次数 2，捕获 614，Boss 1，与单局结果一致。暂停状态时间曾在两次独立读取中精确保持 `121.18399999999654` 秒。

试玩后继续修正：连击回能封顶、阶段鱼量上限、激光短点按、Boss 控制成功提示、紧贴轮廓的碰撞、计时取整、缓存光照、合批弹道、低帧率 DPR 降级及暂停/结算停止持续重绘。对应回归均已补充。

## 浏览器覆盖

- 真实 mouse click / hold / drag / release，以及 Chromium 原生 CDP Touch start / move / cancel。
- 四种武器产生不同弹道/光束，并实际消耗能量；五种道具按钮改变库存和持续时间。
- 暂停/恢复、声音设置、转屏提示、后台可见性处理。自动化的 visibilitychange 检查会模拟 hidden 状态，不能代替物理手机锁屏测试。
- 375×667、390×844、393×852、430×932 竖屏提示；对应 667×375、844×390、852×393、932×430 横屏。
- 生产版额外检查 740×360、915×412、1280×720。
- 所测横屏的 9 个主要操作按钮均至少 44×44 CSS px，无重叠、出界或页面横向溢出。
- 每浏览器连续完成 7 次加速 UI 流程，结算只记账一次，重开清空临时状态；重新加载保留解锁/声音/金库/统计。
- 插桩原生 RAF 请求/取消，始终仅一个待执行 RAF；原生监听注册在重开前后数量不变。
- Chromium 7 局复用一个 AudioContext；Windows WebKit 在未提供可构造音频上下文时走静音降级，无未处理拒绝。
- 新鲜隔离上下文中 Console error、pageerror、unhandledrejection 均为 0。

## 压力场景

固定随机种子；保持 100 鱼（含 Boss）、补足至 180 弹丸、280 粒子，叠加 Frenzy。预热 2.2 秒后，每 1.1 秒采样一次，共 6 次。实体数随命中/过期变化，但持续补充且不能突破池上限。

| 环境 | 屏幕 DPR / Canvas DPR | 六次 RAF FPS | 平均 | 最后采样 CPU 更新+绘制 P95 |
|---|---|---|---:|---:|
| Chromium | 3 / 自适应 1.5 | 60, 60, 60, 60, 60, 60 | 60 | 1 ms |
| Windows WebKit | 1 / 1 | 51, 59, 51, 52, 60, 58 | 55 | 1 ms |

最后观察到 Chromium 100 鱼 / 163 弹丸 / 267 粒子，WebKit 100 / 176 / 272，真实待执行 RAF 均为 1。常规航行软上限最高为 64，明显低于压力场景。

**范围限制：**这不是物理 iPhone / Android 的性能报告。Windows WebKit 高 DPR 软件合成的空场景曾仅约 23 FPS，DPR 3 极限场景约 19 FPS，即使更新+绘制命令耗时约 1ms；DPR 1 下 100 鱼约 64 FPS。因此保留高 DPR 功能测试记录，WebKit 性能测量使用 DPR 1，并不宣称所有设备 60 FPS。关闭同时运行的测试页面后采样，避免无关测试窗口争用本机资源。低性能设备会自动降低 Canvas 分辨率，DOM 控件不降分辨率。

## 生产静态验证

Node 内置 HTTP 服务将 `dist/` 挂在 `/reef/` 子目录。HTML、JS、CSS 均成功加载，没有 404；开始游戏后断网，射击、计时、暂停/继续和手册正常。生产环境不存在 `window.__tidebreak` 或开发性能面板。三个 Android 横屏尺寸通过。静态产物约 270KB JS / 14KB CSS（gzip 约 88KB / 4KB），无外部图片、字体或音频下载。

## 最终自检 15 问

| 问题 | 结论与依据 |
|---|---|
| 1. 不知道项目的人能否直接启动？ | 能。README 提供 Node 要求、安装、启动和静态预览命令。 |
| 2. 手机上能否正常操作？ | 所测移动视口及原生触控事件通过；物理设备系统手势和持续帧率仍需真机测试。 |
| 3. 前 10 秒是否知道干什么？ | 首页直接说明按住/拖动，开局海面有短教学，立即出现低 HP 鱼。 |
| 4. 是否存在实际策略选择？ | 有。清群维持 Combo、存能追踪 Boss、冻结抵挡冲刺、选择高价值鱼。 |
| 5. 四武器是否真的不同？ | 是。单发、扇面、连续穿透、范围减速；自动化验证实际效果和消耗。 |
| 6. 鱼类是否有不同玩法？ | 是。装甲朝向、无敌周期、膨胀、加速、冲刺、宝物、Boss 弱点都有独立验证。 |
| 7. Combo 是否有意义？ | 是。提高金币和回能，有里程碑反馈；回能封顶防止经济失控。 |
| 8. Boss 是否不仅是血条？ | 是。腹部、尾部、冲刺、召唤与半血狂暴；战术模拟均能击败。 |
| 9. 整局是否有起伏？ | 是。四密度阶段、随机事件顺序、两次 Boss 机会、Final Rush 与末秒倒计时。 |
| 10. 30 秒后是否只剩重复点击？ | 特殊鱼与事件从此阶段加入；主动道具和 Boss 引入目标取舍。趣味性的最终判断仍需要新玩家反馈。 |
| 11. 是否存在严重性能问题？ | 在报告所列环境和连续压力采样中无持续低帧率；高 DPR Windows WebKit 软件合成限制已明确记录。 |
| 12. Console 是否干净？ | 新鲜开发回归和生产检查无错误或未处理拒绝。开发过程中临时 HMR 错误已修复，不计为最终结果。 |
| 13. Build 是否成功？ | 是，生产静态测试先执行完整 TypeScript + Vite 构建。 |
| 14. 是否有影响体验的 TODO？ | src / tests 无 TODO、FIXME、placeholder、mock 标记；主要机制均已完成。 |
| 15. 是否能给别人试玩链接？ | 可通过局域网预览，或把 dist 上传任意静态服务；未擅自创建公网发布。 |

## 交付边界

没有物理手机实测证据，也没有以软件 WebKit 代称 iOS Safari 真机测试。未新增服务端、多人房间或付费系统。当前内容覆盖此次请求的完整单机航行；后续真机反馈属于发布设备验证，不隐藏在完成声明中。
