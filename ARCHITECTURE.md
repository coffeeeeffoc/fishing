# 技术架构

## 模块与数据流

- `src/game/config.ts`：集中默认平衡参数、鱼/武器/事件/道具表与资源上限。
- `src/game/engine.ts`：独立于 DOM 和 React 的单局状态、时间、伤害、奖励、对象池和快照。
- `src/game/systems.ts`：鱼移动、朝向、装甲/弱点、固定网格、扫掠椭圆碰撞、对象池。
- `src/game/encounters.ts`：随机事件队列、密度、群游、Boss 冲刺/召唤。
- `src/game/weapons.ts`：不同弹道、持续激光、自动吸附、道具。
- `src/game/renderer.ts`：缓存程序化插画、海底、金币，绘制移动、受击、粒子、弹道和炮台。
- `src/game/audio.ts`：用户手势解锁 Web Audio，音效节流、最大并发、静音、暂停与释放。
- `src/game/storage.ts`：校验存档、异常降级、结算合并；无法访问存储不影响游玩。
- `src/components/GameCanvas.tsx`：单一 RAF、Pointer Capture、坐标映射、ResizeObserver、后台与转屏处理。
- `src/App.tsx`：最多 10 Hz 的 React HUD、开始/暂停/结果/设置/手册、一次性结算保护。
- `tests/`：纯逻辑、完整模拟、浏览器交互/尺寸/生命周期/压力、生产静态子目录测试。

输入修改游戏的瞄准与开火状态；游戏循环按 delta time 更新模拟，渲染器读取模拟；React 消费低频快照。鱼、子弹和特效都不通过 React 元素逐帧更新。

## 生命周期

同一 Game 实例完成开始 → 游玩 → 暂停/恢复 → 结算 → 重开。每次开始清空存活对象与临时奖励，复用池内存，保留已解锁武器。暂停清掉连续射击和光束，不推进游戏时间；页面隐藏、失焦、转为竖屏时自动暂停。恢复重置上一帧时钟，不补算后台时间。

RAF 使用最多 100ms 的外层 delta，并以 1/60 秒分步。暂停和结算保留单一调度但停止持续重绘；尺寸或模式变化时更新一次画面。卸载时取消 RAF、断开 ResizeObserver、移除 8 个 Canvas/全局事件，App 单独清理键盘事件与 AudioContext。React StrictMode 的重复挂载也覆盖在浏览器测试内。

解锁立即写入存档；完整结算由 App 的已结算标志保护，只记账一次，重开复位该标志。存档版本为 1，数值必须有限、非负并限制范围；设置必须为布尔值，武器必须来自允许列表。读取 JSON 或 LocalStorage 失败时使用安全默认值，写入失败显示提示。

## 性能

- 逻辑尺寸固定 1200×675，Canvas 等比适应实际空间，点击坐标应用同样的比例与偏移。
- Canvas DPR 最大 2；连续低 FPS 时按 0.5 阶梯下降，最低 1。单次挂载只降低，以避免画质震荡。React 控件仍使用屏幕原生分辨率。
- 网格 100 逻辑像素。鱼占据其外接边界覆盖的所有格子，弹丸只查询扫掠区域覆盖格，窄阶段计算最早椭圆交点；穿透激光使用同一 broad phase。
- 硬上限：100 鱼、180 弹丸、280 粒子、75 金币；浮动文字 24 条。常规生成另有阶段软上限。
- 对象死亡标记闲置，下一次获取覆盖字段；离屏、过期弹丸与鱼回收，避免无限增长。
- 海底与光照预绘；鱼使用紧贴轮廓的缓存图；金币缓存一次；同种弹道合批；小粒子使用轻量方形闪点。
- 开发统计给出 RAF FPS、更新+绘制命令耗时和对象数量。耗时不等同于 GPU/合成器端到端延迟，不能用 1ms 绘制命令耗时宣称真机 1000 FPS。
- 开发环境 `window.__tidebreak` 仅供隔离浏览器夹具检查；生产构建完全移除入口和性能面板。

## 验证边界

逻辑测试不依赖浏览器或音频。浏览器脚本使用独立上下文，原生 Pointer/Touch 输入、截图、异常与真实 RAF/事件注册插桩。长局逻辑和生命周期使用加速模拟；另有实际计时的手动浏览器流程。生产测试使用 Node 内置 HTTP 临时服务挂载 `/reef/`，验证相对资源、断网后游玩以及无调试入口。

Windows WebKit 的高 DPR 软件合成不是 iPhone GPU。其 DPR 3 空场景亦出现低帧率，故功能兼容记录与 DPR 1 性能记录分开；真实设备性能不作已验证声明。
